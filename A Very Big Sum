#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
    long long int sum=0,a[1000],n,i;
    scanf("%d\n",&n);
    for(i=0;i<n;i++)
    {
        scanf("%lld ",&a[i]);
        sum=sum+a[i];
    }
    printf("%lld",sum);
    return 0;
}
