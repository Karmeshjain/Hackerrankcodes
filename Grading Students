#include <bits/stdc++.h>

using namespace std;

int main()
{
    int id;
    cin>>id;
    int a[id];
    for(int i=0;i<id;i++)
     {   
         int res=0;
         cin>>a[i];
        if(a[i]<38)
        {cout<<a[i]<<"\n";
        continue;}
        int k=a[i];
       while(k%5!=0)
       {
           ++k;
           ++res;
       }
     if(res<3)
     cout<<k<<"\n";
     else
         cout<<a[i]<<"\n";
    }
}
